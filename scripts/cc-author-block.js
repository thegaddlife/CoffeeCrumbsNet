$(function() {

    $(document).on("ready", function() {
        var authorBlockDiv = $(".author-block:visible");
        if (authorBlockDiv.length)
            ccAuthorBlock.InitPlugin(authorBlockDiv);
    });

});

var ccAuthorBlock = {

    InitPlugin: function(block) {

        // find the author id

        // get the author


    }
}